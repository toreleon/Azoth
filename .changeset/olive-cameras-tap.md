---
"@toreleon/azoth": patch
---

Expand the VNDirect `CompanyProfile` type with fields the endpoint already returns: `enAddress`, `logo`, `employees` and `branches`. Type-only, additive — existing consumers are unaffected.
